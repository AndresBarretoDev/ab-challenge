import { AppRouter } from './routers/AppRouter'

function App() {
  /** para mantener un inicio limpio, se crea un componente que maneje las rutas
   * para este ejemplo
   */
  return <AppRouter />
}

export default App
